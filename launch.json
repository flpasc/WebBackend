{
	"version": "0.2.0",
	"configurations": [
		{
			"type": "node",
			"request": "launch",
			"name": "Launch Program",
			"skipFiles": ["<node_internals>/**"],
			"program": "${workspaceFolder}/your-app.js", // Adjust this to your main file's path
			"restart": true // Optional: Restart the debugger on code changes
		}
	]
}
